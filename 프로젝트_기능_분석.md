# TeamProject_Cucumber 프로젝트 기능 분석 문서

> **프로젝트명**: Cucumber (오이)
> **프로젝트 유형**: 중고거래 및 이웃 커뮤니티 플랫폼
> **작성일**: 2026-01-05
> **현재 브랜치**: dev.hyemin

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택](#기술-스택)
3. [프로젝트 구조](#프로젝트-구조)
4. [구현된 주요 기능](#구현된-주요-기능)
5. [핵심 컴포넌트](#핵심-컴포넌트)
6. [상태 관리](#상태-관리)
7. [API 서비스](#api-서비스)
8. [라우팅 구조](#라우팅-구조)
9. [데이터 타입](#데이터-타입)
10. [개발 현황](#개발-현황)

---

## 프로젝트 개요

**Cucumber**는 이웃 간 중고거래와 커뮤니티 기능을 제공하는 모바일 웹 플랫폼입니다. 당근마켓과 유사한 서비스로, 지역 기반 거래와 커뮤니티 활동을 지원합니다.

### 주요 목표
- 지역 기반 중고거래 플랫폼 구축
- 사용자 간 신뢰도 관리 시스템
- 관심 목록 기반 추천 알고리즘
- 포괄적인 신고 및 분쟁 처리 시스템

---

## 기술 스택

### 프론트엔드 핵심
| 기술 | 버전 | 용도 |
|------|------|------|
| React | 19.1.1 | UI 라이브러리 |
| TypeScript | ~5.9.3 | 정적 타입 언어 |
| Vite | 7.1.12 | 빌드 도구 및 개발 서버 |
| React Router | 7.9.5 | 클라이언트 라우팅 |
| Zustand | 5.0.8 | 상태 관리 |

### 스타일링
| 기술 | 버전 | 용도 |
|------|------|------|
| Tailwind CSS | 4.1.16 | 유틸리티 CSS 프레임워크 |
| tailwind-merge | 3.3.1 | Tailwind 클래스 병합 |

### 추가 라이브러리
- **React Icons** (5.5.0): 아이콘 컴포넌트
- **React Transition Group** (4.4.5): 페이지 전환 애니메이션

### 외부 API
- **SGIS API** (통계청 지도 API): 지역 정보 조회 서비스

---

## 프로젝트 구조

```
TeamProject_Cucumber/
├── Team_Cucumber/
│   └── frontend/                    # 메인 프론트엔드 프로젝트
│       ├── src/
│       │   ├── pages/              # 페이지 컴포넌트 (12개)
│       │   ├── components/         # 재사용 컴포넌트 (60+개)
│       │   │   ├── common/        # 공통 컴포넌트
│       │   │   ├── mypage/        # 마이페이지 컴포넌트
│       │   │   └── reports/       # 신고 관련 컴포넌트
│       │   ├── stores/            # Zustand 상태관리
│       │   ├── services/          # API 서비스
│       │   ├── types/             # TypeScript 타입
│       │   ├── data/              # Mock 데이터
│       │   ├── constants/         # 상수
│       │   ├── hooks/             # 커스텀 훅
│       │   ├── utils/             # 유틸리티 함수
│       │   ├── layout/            # 레이아웃
│       │   ├── routers/           # 라우터 설정
│       │   └── assets/            # 정적 파일
│       └── package.json
└── backend/                        # 백엔드 (개발 예정)
```

---

## 구현된 주요 기능

### 1. 인증 및 사용자 관리 🔐

#### 구현된 기능
- 로그인/회원가입 페이지
- 사용자 프로필 관리
  - 닉네임, 전화번호, 이메일
  - 프로필 이미지
  - 가입일 정보
- 지역 정보 등록
- localStorage 기반 인증 상태 유지

#### 관련 파일
- `src/pages/LoginPage.tsx`
- `src/pages/LocationPage.tsx`
- `src/stores/useAuthStore.ts`

---

### 2. 홈 페이지 및 거래 🏠

#### 구현된 기능
- **상품 목록 표시**
  - 지역별 상품 필터링
  - 카테고리별 분류 (전체, 중고거래, 방금 전)
  - 상품 검색 기능

- **상품 카드**
  - 상품 이미지, 제목, 가격 표시
  - 좋아요, 채팅 수 표시
  - 상태 표시 (판매중, 예약중, 거래완료)

- **물건 팔기**
  - 모달을 통한 판매 옵션 선택

- **바텀시트 메뉴**
  - 관심있음/없음 토글
  - 게시글 숨기기
  - 신고하기

#### 관련 파일
- `src/pages/HomePage.tsx`
- `src/components/common/ProductCard.tsx`
- `src/components/common/BottomSheet.tsx`

---

### 3. 위치 설정 📍

#### 구현된 기능
- **3단계 지역 선택**
  1. 시도(도시) 선택
  2. 시군구(구역) 선택
  3. 읍면동(동네) 선택

- **SGIS API 연동**
  - 실시간 지역 정보 조회
  - API 토큰 자동 갱신 (23시간 캐시)
  - 계층적 지역 데이터 로딩

#### 관련 파일
- `src/pages/LocationPage.tsx`
- `src/services/sgisService.ts`
- `src/stores/useLocationStore.ts`

---

### 4. 마이페이지 (나의 오이) 👤

> **최근 구현**: SCRUM-7 - 레이아웃 구현 완료

#### 4.1 메인 페이지 (`/mypage`)

**프로필 섹션**
- 사용자 프로필 카드
- 매너 온도계 (신뢰도 점수)
- 프로필 편집 버튼

**서비스 배너 섹션**
- 중고거래
- 최근 본 글
- 혜택

**메뉴 구성**
1. **나의 거래**
   - 판매내역
   - 구매내역
   - 가격 찾기
   - 가계부

2. **나의 관심**
   - 관심목록
   - 키워드 알림 설정

3. **나의 활동**
   - 동네생활 글

4. **설정**
   - 웹 설정

5. **고객지원**
   - 공지사항
   - 고객센터
   - 의견 남기기
   - 약관 및 정책

#### 4.2 프로필 페이지 (`/mypage/profile`)

**사용자 정보**
- 사용자 ID
- 가입일
- 동네 인증 정보
- 매너 온도계

**판매 활동**
- 최근 판매 물품 (최대 3개)
- 리뷰 정보
- 활동 배지

#### 4.3 관심목록 페이지 (`/mypage/likelist`)

> **최근 구현**: SCRUM-5 - 추천 알고리즘 구현

**찜한 상품 목록**
- 상품 이미지, 제목, 가격
- 좋아요 해제 기능
- 상품 상태 표시

**추천 알고리즘**
- **카테고리 기반 추천**: +10점
- **태그/키워드 기반 추천**: +2점
- 점수 순 정렬
- 최소 점수: 2점 이상

**추천 상품 섹션**
- 상위 6개 추천 상품 표시
- 실시간 업데이트

#### 4.4 거래 관련 페이지

| 페이지 | 경로 | 기능 |
|--------|------|------|
| 판매내역 | `/mypage/sales-history` | 판매한 상품 목록 |
| 구매내역 | `/mypage/purchase-history` | 구매한 상품 목록 |
| 가계부 | `/mypage/account-book` | 거래 내역 관리 |
| 동네활동 | `/mypage/neighborhood-activity` | 커뮤니티 활동 내역 |

#### 4.5 설정 및 기타

| 페이지 | 경로 | 기능 |
|--------|------|------|
| 키워드 알림 | `/mypage/keyword-notification-settings` | 키워드 알림 관리 |
| 키워드 편집 | `/mypage/keyword-edit` | 키워드 추가/삭제 |
| 웹 설정 | `/mypage/settings` | 앱 설정 |
| 의견 남기기 | `/mypage/feedback` | 피드백 제출 |
| 약관 및 정책 | `/mypage/terms-and-policies` | 서비스 약관 |

#### 관련 파일
```
src/
├── pages/
│   └── mypage/
│       ├── MyCucumberPage.tsx           # 메인
│       ├── ProfilePage.tsx              # 프로필
│       ├── LikeListPage.tsx            # 관심목록
│       ├── SalesHistoryPage.tsx        # 판매내역
│       └── ... (기타 12+ 페이지)
├── components/mypage/
│   ├── ProfileCard.tsx                 # 프로필 카드
│   ├── MannerTemperatureCard.tsx       # 온도계
│   ├── LikedProductItem.tsx            # 찜한 상품
│   └── ... (15+ 컴포넌트)
└── utils/
    └── RecommendationCalculation.ts    # 추천 알고리즘
```

---

### 5. 신고 시스템 🚨

> **포괄적인 신고 카테고리 시스템 구축 완료**

#### 5.1 주요 신고 카테고리 (6개)

| 카테고리 | 설명 | 세부 항목 수 |
|----------|------|--------------|
| 거래 금지 물품 | 법적으로 거래 불가한 물품 | 15+ |
| 전문판매업자 | 사업자 의심 행위 | 3 |
| 사기 | 거래 사기 행위 | 4 |
| 대리 결제/구매 | 대리 거래 행위 | 2 |
| 부적절한 행위 | 규칙 위반 행위 | 8 |
| 사용자 신고 | 프로필/닉네임 문제 | 3 |

#### 5.2 거래 금지 물품 세부 항목

1. 생명이 있는 동물
2. 의약품/의료기기
3. 상표권 침해 의심 상품
4. 저작권 침해 의심 상품
5. 청소년 유해 약물
6. 건강기능식품 가이드라인 위반
7. 담배
8. 주류
9. 식품 위생 법규 위반
10. 위조/변조 화폐
11. 총포/도검/화약류
12. 불법 복제 소프트웨어
13. 개인정보 거래
14. 계정 거래
15. 기타 법률 위반 상품

#### 5.3 사기 관련 신고

- 판매자 미배송
- 구매자 미결제
- 허위 매물
- 외부 거래 유도

#### 5.4 부적절한 행위

- 중복 게시글
- 허위 매물
- 가격 재판매
- 광고/홍보
- 욕설/혐오 발언
- 거래 완료 후 가격 변경
- 원치 않는 채팅
- 부적절한 성적 행위

#### 5.5 신고 기능

**검색 및 필터링**
- 신고 카테고리 검색
- 신고 항목 목록 필터링

**신고 제출**
- 피신고자/상품 정보 저장
- 신고 사유 입력
- 증거 자료 첨부
- 사용자 차단 옵션

**UI 컴포넌트**
- 모달 기반 신고 제출
- 단계별 신고 프로세스
- 신고 완료 확인

#### 관련 파일
```
src/
├── pages/
│   └── reports/
│       ├── ReportsPage.tsx             # 메인
│       ├── BansPage.tsx               # 거래 금지
│       ├── FraudsPage.tsx             # 사기
│       └── ... (30+ 세부 페이지)
├── components/reports/
│   ├── ReportsHeader.tsx              # 헤더
│   ├── ReportsList.tsx                # 목록
│   ├── ReportsSearchInput.tsx         # 검색
│   ├── ReportsModal.tsx               # 모달
│   └── ... (10+ 컴포넌트)
└── data/reports.ts                     # 신고 데이터
```

---

### 6. 기타 페이지

| 페이지 | 경로 | 설명 |
|--------|------|------|
| 랜딩페이지 | `/` | 초기 진입 페이지 |
| 환영페이지 | `/welcome` | 가입 가이드 |
| 카테고리 | `/category` | 거래 카테고리 선택 |
| 테스트 | `/test/*` | 개발자 테스트 페이지 |

---

## 핵심 컴포넌트

### 공통 컴포넌트 (`components/common/`)

| 컴포넌트 | 설명 | 주요 Props |
|----------|------|------------|
| **Header** | 페이지 상단 네비게이션 | title, leftAction, rightAction |
| **Button** | 기본 버튼 | variant, size, onClick |
| **ProductCard** | 상품 카드 | product, onClick |
| **Modal** | 다목적 모달 | isOpen, onClose, children |
| **BottomSheet** | 하단 슬라이드 메뉴 | isOpen, onClose, items |
| **Toast** | 알림 메시지 | message, type, duration |
| **Checkbox** | 체크박스 | checked, onChange, label |
| **Radio** | 라디오 버튼 | value, onChange, options |
| **Tabs** | 탭 네비게이션 | tabs, activeTab, onTabChange |
| **Textarea** | 텍스트 입력 | value, onChange, placeholder |
| **ToggleButton** | 토글 버튼 | checked, onChange |
| **Icon** | 아이콘 시스템 | name, size, color |
| **Portal** | React Portal | children |
| **PageTransition** | 페이지 전환 애니메이션 | children, location |

### 디자인 패턴

**Modal 컴포넌트 - Compound Component 패턴**
```tsx
<Modal isOpen={isOpen} onClose={onClose}>
  <Modal.Header>제목</Modal.Header>
  <Modal.Body>내용</Modal.Body>
  <Modal.Footer>
    <Button>확인</Button>
  </Modal.Footer>
</Modal>
```

---

## 상태 관리

### Zustand Stores

#### useAuthStore
```typescript
interface AuthStore {
  // 상태
  user: User | null;
  isLoggedIn: boolean;

  // 액션
  login: (user: User) => void;
  logout: () => void;
  updateProfile: (updates: Partial<User>) => void;
  setLocation: (location: string) => void;
}

// localStorage 자동 저장: 'auth-storage'
```

#### useLocationStore
```typescript
interface LocationStore {
  selectedLocation: Location | null;
  setLocation: (location: Location) => void;
  clearLocation: () => void;
}
```

#### useToastStore
```typescript
interface ToastStore {
  toasts: Toast[];
  addToast: (message: string, type?: ToastType) => void;
  removeToast: (id: string) => void;
}
```

---

## API 서비스

### SGIS Service (통계청 지도 API)

#### 주요 메서드

```typescript
// 1. 인증 토큰 발급 (23시간 캐시)
getAccessToken(): Promise<string>

// 2. 시도(도시) 목록 조회
getSidoList(): Promise<SidoItem[]>

// 3. 시군구(구역) 목록 조회
getSigunguList(sidoCode: string): Promise<SigunguItem[]>

// 4. 읍면동(동네) 목록 조회
getEupmyeondongList(sigunguCode: string): Promise<EupmyeondongItem[]>
```

#### API 엔드포인트
- **베이스 URL**: `https://sgisapi.kostat.go.kr`
- **프록시 설정**: 개발 환경에서 CORS 우회

#### 인증 정보
```typescript
const SGIS_CONFIG = {
  consumer_key: "발급받은 키",
  consumer_secret: "발급받은 시크릿"
}
```

---

## 라우팅 구조

### 주요 라우트

```
/ (LandingPage)
├── /welcome (WelcomePage)
├── /login (LoginPage)
├── /signup/location (LocationPage)
├── /home (HomePage)
├── /category (CategoryPage)
│
├── /mypage (MyCucumberPage)
│   ├── /mypage/profile
│   ├── /mypage/likelist
│   ├── /mypage/sales-history
│   ├── /mypage/purchase-history
│   ├── /mypage/account-book
│   ├── /mypage/neighborhood-activity
│   ├── /mypage/keyword-notification-settings
│   ├── /mypage/keyword-edit
│   ├── /mypage/settings
│   ├── /mypage/feedback
│   └── /mypage/terms-and-policies
│
└── /reports (ReportsPage)
    ├── /reports/bans/*
    │   ├── living-animals
    │   ├── medicine-medical-devices
    │   ├── trademark-infringement
    │   └── ... (15+ 항목)
    ├── /reports/professional
    ├── /reports/frauds/*
    │   ├── seller-no-shipping
    │   ├── buyer-no-payment
    │   └── ...
    ├── /reports/proxy
    ├── /reports/inappropriates/*
    │   ├── duplicate-post
    │   ├── profanity
    │   └── ...
    ├── /reports/users/*
    │   ├── profile-image
    │   ├── nickname
    │   └── political-religious
    └── /reports/dispute

/test (TestPage)
├── /test/Hemin
├── /test/Nara
├── /test/Joowon
└── /test/icons
```

### 라우터 설정
- **라이브러리**: React Router v7
- **페이지 전환**: React Transition Group 사용
- **보호된 라우트**: 인증 필요 여부에 따른 리다이렉션

---

## 데이터 타입

### Product (상품)
```typescript
interface Product {
  id: number;
  title: string;
  price: number;
  image: string;
  isLiked: boolean;
  category: string;
  tags: string[];
  likeCount?: number;
  chatCount?: number;
  location?: string;
  timeAgo?: string;
  status?: "on-sale" | "reserved" | "sold-out";
}
```

### User (사용자)
```typescript
interface User {
  id: string;
  nickname: string;
  phone: string;
  location: string;
  profileImage?: string;
  email?: string;
  createdAt?: Date;
}
```

### Review (리뷰)
```typescript
interface Review {
  id: number;
  reviewerName: string;
  role: string; // "판매자" | "구매자"
  location: string;
  timeAgo: string;
  content: string;
}
```

### ReportInfo (신고 정보)
```typescript
interface ReportInfo {
  opponent_id?: string;      // 피신고자 ID
  product_id?: string;        // 신고 상품 ID
  report_text: string;        // 신고 사유
  report_field_id?: string;   // 신고 분야 ID
  report_type_id?: string;    // 신고 유형 ID
}
```

### Location (위치)
```typescript
interface SidoItem {
  cd: string;    // 시도 코드
  addr_name: string;  // 시도명
}

interface SigunguItem {
  cd: string;    // 시군구 코드
  addr_name: string;  // 시군구명
}

interface EupmyeondongItem {
  cd: string;    // 읍면동 코드
  addr_name: string;  // 읍면동명
}
```

---

## 개발 현황

### 팀 구성

| 이름 | 역할 | 주요 작업 |
|------|------|-----------|
| **HyeMin** (혜민) | 프론트엔드 | 마이페이지, UI 컴포넌트 |
| **Nara** (나라) | 프론트엔드 | 신고 시스템, 헤더, 탭 |
| **Joowon** (주원) | 프론트엔드 | 위치 API, 라우팅 |

### 최근 커밋 내역

```
637f32f - SCRUM-7 feat: 나의 오이 페이지 레이아웃 구현
a821619 - Merge branch 'frontend' into dev.hyemin
831262e - commit
761e1fe - Merge pull request #33
1f6190e - feat: 관심목록 페이지 구현
```

### 현재 브랜치
- **main**: 프로덕션 브랜치
- **frontend**: 프론트엔드 개발 브랜치
- **dev.hyemin**: 혜민 개발 브랜치 (현재)

### 코드 통계

| 항목 | 수량 |
|------|------|
| TypeScript/TSX 파일 | 164개 |
| 컴포넌트 | 60+ 개 |
| 페이지 | 12개 (메인) + 30+ (신고) |
| Zustand Store | 3개 |
| API 서비스 | 1개 (SGIS) |

---

## 추천 알고리즘 상세

### 구현 위치
`src/utils/RecommendationCalculation.ts`

### 알고리즘 로직

```typescript
function getRecommendedProducts(
  likedProducts: Product[],
  allProducts: Product[]
): Product[] {
  // 1. 사용자가 찜한 카테고리 수집
  const likedCategories = likedProducts.map(p => p.category);

  // 2. 사용자가 찜한 태그 수집
  const likedTags = likedProducts.flatMap(p => p.tags || []);

  // 3. 각 상품에 점수 부여
  const scoredProducts = allProducts
    .filter(p => !likedProducts.some(liked => liked.id === p.id))
    .map(product => {
      let score = 0;

      // 카테고리 일치: +10점
      if (likedCategories.includes(product.category)) {
        score += 10;
      }

      // 태그 일치: 각 +2점
      product.tags?.forEach(tag => {
        if (likedTags.includes(tag)) {
          score += 2;
        }
      });

      return { product, score };
    });

  // 4. 점수 순 정렬 및 필터링
  return scoredProducts
    .filter(item => item.score >= 2)  // 최소 점수
    .sort((a, b) => b.score - a.score)
    .slice(0, 6)  // 상위 6개
    .map(item => item.product);
}
```

### 사용 예시
```typescript
// LikeListPage.tsx
const recommendedProducts = getRecommendedProducts(
  likedProducts,
  allProducts
);
```

---

## Mock 데이터

### mypage.ts

#### 배너 광고 (mockItems)
```typescript
const mockItems = [
  {
    id: 1,
    title: "중고거래",
    description: "이웃과 따뜻한 거래를 해보세요.",
    icon: "🛒",
    link: "/trade"
  },
  // ... 4개
];
```

#### 사용자 데이터 (mockUsers)
```typescript
const mockUsers = [
  {
    id: "woody_cucumber",
    nickname: "우디",
    profileImage: "/assets/woody.png",
    mannerTemperature: 36.5,
    // ...
  },
  // 우디, 버즈, 제시 (3명)
];
```

#### 상품 데이터
- **likedProducts**: 찜한 상품 목록 (10+개)
- **allProducts**: 전체 상품 목록 (추천용, 20+개)

### reports.ts

#### 신고 카테고리 (reports)
```typescript
const reports = [
  {
    id: "bans",
    category: "거래 금지 물품이에요",
    items: 15
  },
  // ... 6개 주요 카테고리
];
```

#### 세부 신고 항목
- **bans**: 15+ 항목 (거래 금지 물품)
- **frauds**: 4 항목 (사기)
- **inappropriates**: 8 항목 (부적절한 행위)
- **users**: 3 항목 (사용자 신고)

---

## 스타일링

### Tailwind CSS 설정

#### 반응형 디자인
- **모바일 우선**: 최대 너비 768px
- **화면 중앙 정렬**: `mx-auto max-w-screen-md`

#### 주요 색상 팔레트
```css
/* 주요 색상 */
--primary: orange-500 (#f97316)
--background: white
--text-primary: gray-900
--text-secondary: gray-500
--border: gray-200

/* 상태 색상 */
--success: green-500
--error: red-500
--warning: yellow-500
```

#### 커스텀 유틸리티
```css
.clamp-2-text {
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

### 애니메이션

#### 페이지 전환 (`page_transition.css`)
```css
.page-enter {
  opacity: 0;
  transform: translateX(100%);
}

.page-enter-active {
  opacity: 1;
  transform: translateX(0);
  transition: all 300ms ease-in-out;
}

.page-exit {
  opacity: 1;
  transform: translateX(0);
}

.page-exit-active {
  opacity: 0;
  transform: translateX(-100%);
  transition: all 300ms ease-in-out;
}
```

---

## 향후 개발 계획

### 백엔드 개발
- [ ] REST API 엔드포인트 구축
- [ ] 데이터베이스 설계 (사용자, 상품, 거래, 신고)
- [ ] 인증/인가 시스템 (JWT)
- [ ] 파일 업로드 서비스

### 추가 기능
- [ ] 실시간 채팅 (WebSocket)
- [ ] 상품 상세 페이지
- [ ] 결제 시스템 통합
- [ ] 푸시 알림
- [ ] 이미지 업로드/편집
- [ ] 리뷰 작성 및 관리
- [ ] 거래 내역 상세
- [ ] 관심 키워드 알림

### 성능 최적화
- [ ] 이미지 lazy loading
- [ ] 코드 스플리팅
- [ ] 상태 관리 최적화
- [ ] API 캐싱 전략

### 테스트
- [ ] 단위 테스트 (Jest)
- [ ] 컴포넌트 테스트 (React Testing Library)
- [ ] E2E 테스트 (Playwright)

---

## 기술 결정 사항

### 왜 Zustand를 선택했는가?
- Redux보다 간단한 API
- 보일러플레이트 코드 최소화
- TypeScript 지원 우수
- localStorage 영속화 쉬움

### 왜 Vite를 선택했는가?
- 빠른 개발 서버 시작
- HMR (Hot Module Replacement) 성능 우수
- ESM 기반 빌드
- 플러그인 생태계 풍부

### 왜 Tailwind CSS를 선택했는가?
- 유틸리티 기반으로 빠른 개발
- 일관된 디자인 시스템
- 반응형 디자인 쉬움
- 번들 크기 최적화 (PurgeCSS)

---

## 프로젝트 실행 방법

### 설치
```bash
cd Team_Cucumber/frontend
npm install
```

### 개발 서버 실행
```bash
npm run dev
```

### 빌드
```bash
npm run build
```

### 프리뷰
```bash
npm run preview
```

---

## 환경 변수

### 필요한 환경 변수
```env
VITE_SGIS_CONSUMER_KEY=your_consumer_key
VITE_SGIS_CONSUMER_SECRET=your_consumer_secret
```

---

## 라이선스 및 참고

이 프로젝트는 교육 목적의 팀 프로젝트입니다.

**참고한 서비스**: 당근마켓 (Carrot Market)

---

**문서 작성일**: 2026-01-05
**문서 버전**: 1.0
**최종 업데이트**: SCRUM-7 완료 시점
